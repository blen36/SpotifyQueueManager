# 🎵 Spotify Queue Manager

**Spotify Queue Manager** — это веб-приложение на Django, которое позволяет создавать **совместные музыкальные очереди** для поездок, вечеринок и групповых мероприятий.

Один пользователь (хост) подключает свой Spotify-аккаунт, а остальные участники комнаты могут **добавлять треки в очередь**, **голосовать за пропуск** и **управлять воспроизведением** — без необходимости иметь Spotify Premium.

---

## 🚀 Основная идея

- 👤 **Хост** подключает Spotify и управляет воспроизведением
- 👥 **Гости** подключаются к комнате по коду
- 🎶 Любой участник может искать треки и добавлять их в очередь
- ⏭ Пропуск трека возможен через голосование
- 🌐 Управление происходит в реальном времени через веб-интерфейс

---

## 🧩 Основной функционал

### Комнаты
- Создание комнаты с уникальным кодом
- Подключение гостей по коду комнаты
- Сессии без регистрации для гостей

### Spotify
- OAuth-авторизация через Spotify API
- Получение текущего трека
- Play / Pause / Skip
- Добавление треков в очередь

### Очередь и голосование
- Поиск треков через Spotify Search API
- Добавление треков в очередь воспроизведения
- Голосование за пропуск текущего трека
- Настраиваемое количество голосов для skip

### Интерфейс
- Асинхронное обновление через **HTMX**
- Минимальный JavaScript
- Bootstrap + кастомные стили
- Автообновление статуса трека

---

## 🛠 Используемые технологии

- **Python 3**
- **Django 5**
- **Django REST Framework**
- **Spotify Web API**
- **HTMX**
- **Bootstrap 5**
- **PostgreSQL**
- **OAuth 2.0**

---

## 🧱 Архитектура проекта

spotify_jukebox/
│
├── config/ # Django settings, urls, wsgi, asgi
│
├── jukebox/ # Основное приложение
│ ├── models.py # Room, Vote, SpotifyToken
│ ├── views.py # API и HTML-вьюхи
│ ├── spotify_util.py # Работа с Spotify API
│ ├── urls.py # URL-маршруты
│ ├── templates/
│ │ └── jukebox/
│ │ ├── room.html
│ │ ├── home.html
│ │ └── partials/
│ └── static/
│
└── manage.py

yaml

---

## 🔐 Роли пользователей

### Хост
- Создаёт комнату
- Подключает Spotify
- Управляет воспроизведением
- Меняет настройки комнаты

### Гость
- Заходит по коду комнаты
- Добавляет треки в очередь
- Голосует за пропуск

> Гостям **не нужен Spotify Premium**

---

## ⚙️ Как запустить проект локально

### 1. Клонировать репозиторий
```bash
git clone https://github.com/blen36/SpotifyQueueManager.git
cd SpotifyQueueManager
2. Создать виртуальное окружение
python -m venv venv
source venv/bin/activate   # Linux / macOS
venv\Scripts\activate      # Windows
3. Установить зависимости
pip install -r requirements.txt
4. Создать .env файл
env
DJANGO_SECRET_KEY=your-secret-key
DEBUG=True

SPOTIPY_CLIENT_ID=your_spotify_client_id
SPOTIPY_CLIENT_SECRET=your_spotify_client_secret
SPOTIPY_REDIRECT_URI=http://127.0.0.1:8000/redirect

DB_NAME=jukebox_db
DB_USER=postgres
DB_PASSWORD=your_password
DB_HOST=localhost
DB_PORT=5432
5. Применить миграции

python manage.py migrate
6. Создать суперпользователя (для админки)
python manage.py createsuperuser
7. Запустить сервер
python manage.py runserver
Открыть в браузере:
cpp
http://127.0.0.1:8000/
🧪 Как пользоваться
Создать комнату

Подключить Spotify (только хост)

Скопировать код комнаты

Гости заходят по коду

Добавлять треки и управлять очередью 🎧

⚠️ Ограничения
Управление воспроизведением работает только если Spotify открыт на любом устройстве

Для реального воспроизведения нужен Spotify Premium у хоста

Проект рассчитан на учебное / демонстрационное использование

👥 Команда
Проект выполнен командой из 3 человек в рамках учебной работы.
